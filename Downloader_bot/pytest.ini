[tool:pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = src
addopts = 
    --verbose
    --tb=short
    --cov=src
    --cov-report=html
    --cov-report=term-missing
    --cov-fail-under=70
asyncio_mode = auto

